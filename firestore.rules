rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all read and write during development
    // WARNING: Please update these rules before deploying to production
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
